# build outputs
build/
build_test.jar
verify.jar
*.pyc
__pycache__/
installer/

# the jar produced at the repo root by build.py; dist/tracker-improved.jar is
# committed on purpose (it doubles as the incremental build base), so this is
# anchored to the root and does not exclude it
/tracker-improved.jar

# IDE
.classpath
.project
.settings/

# OS
Thumbs.db
.DS_Store

# logs
*.log
